
Cliente
(username, contraseña)

Articulo
(id, precio, nombre, descripcion)
metodos
(insert, select_id, select_all)

Bono
(codigo, fecha, activo/inactivo, porcentaje de descuento)
metodos
(insert, update, select)

Carrito
(id, lista de articulos)
metodos
(insert;al inicio del programa, update, select)

Categoria
(id, nombre)

-----------------------------------------------------------------

1. Creamos clases
    - Atributos privados
    - Getters/Setters
    - str
    - funciones específicas

2. Creamos el esquema de base de datos
    - transformamos los metodos a sql

3. Mi programa principal
    - imports
    - constantes
    - creacion inicial de datos(si no los tengo)
    LOS PARÁMETROS DE CONFIGURACION DE LA BASE DE DATOS DEBEN IR EN MAYÚSCULAS, ES UNA CONSTANTE
        - comprobar si ya existe el esquema
            NO: contruyo el esquema
            Si: no hago nada